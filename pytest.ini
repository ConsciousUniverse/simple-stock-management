[pytest]
DJANGO_SETTINGS_MODULE = ssm.settings_test
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    e2e: end-to-end browser tests (Playwright); deselect with -m "not e2e"
filterwarnings =
    error::django.core.paginator.UnorderedObjectListWarning
